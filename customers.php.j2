<?php
$servername = "192.168.100.70";
$username = "dbuser";
$password = "dbpass";
$dbname = "classicmodels";

// Create connection
$conn = mysqli_connect($servername, $username, $password, $dbname);
// Check connection
if (!$conn) {
    die("Connection failed: " . mysqli_connect_error());
}

$sql = "SELECT customerNumber, contactFirstName, contactLastName FROM customers limit 1";
$result = mysqli_query($conn, $sql);

if (mysqli_num_rows($result) > 0) {
    // output data of each row
    while($row = mysqli_fetch_assoc($result)) {
        echo "id: " . $row["customerNumber"]. " - Name: " . $row["contactFirstName"]. " " . $row["contactLastName"]. "<br>";
    }
} else {
    echo "0 results";
}

mysqli_close($conn);
?>